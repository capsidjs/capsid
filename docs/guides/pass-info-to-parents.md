# Pass information to the parents

`capsid` recommends for each component to pass information to its parents by using *Event Bubbling* of native DOM Events.

Suppose the following example:

```
TBD
```
